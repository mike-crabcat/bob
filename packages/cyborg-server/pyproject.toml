[build-system]
requires = ["hatchling>=1.27.0"]
build-backend = "hatchling.build"

[project]
name = "cyborg-server"
version = "0.1.0"
description = "FastAPI server for Cyborg data service"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
authors = [
  { name = "OpenClaw" },
]
dependencies = [
  "cyborg-core==0.1.0",
  "aiosqlite>=0.20.0",
  "fastapi>=0.115.0",
  "httpx>=0.28.0",
  "jinja2>=3.1.0",
  "pydantic>=2.10.0",
  "python-multipart>=0.0.18",
  "python-jose>=3.3.0",
  "uvicorn>=0.35.0",
  "websockets>=15.0.0",
]

[tool.hatch.build.targets.wheel]
packages = ["cyborg_server"]

[tool.hatch.build]
include = [
  "cyborg_server/**/*.py",
  "cyborg_server/**/*.sql",
  "cyborg_server/**/*.html",
]
