"""Bob Events rehearsal harness (bob-events-plan.md §4.3).

Persona-scripted multi-party scenario driving the REAL pipeline — session
history, silent-turn extraction (tools real, model scripted), the claim
router, the reviser (semantics scripted, state/CAS/wakes real), goal
effects, wake dispatches with the real tool surface, the payment gate, and
the POD stub. Scores information-loss incidents, unnecessary wakes, and
human interventions; the rollout gate is zero information loss across the
scenario + perturbation reruns.
"""
